# Calvin gallery folder

Add image files (e.g. `.jpg`, `.png`) into this folder, then update `manifest.json` to control:

- ordering
- captions (`alt`)
- which images show up in the “Calvin” panel on each page

The site loads the list from `calvin/manifest.json` (no build step).

